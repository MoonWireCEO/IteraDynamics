# AlphaEngine Environment Configuration

# Environment (dev, staging, production)
AE_ENV=dev

# Redis Configuration
REDIS_URL=redis://localhost:6379

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Social Sentiment Feature Flags (disabled by default)
NEWS_SENTIMENT_ENABLED=false
REDDIT_SENTIMENT_ENABLED=false
TWITTER_SENTIMENT_ENABLED=false

# Optional: Twitter API (only needed if TWITTER_SENTIMENT_ENABLED=true)
# TWITTER_BEARER_TOKEN=your_bearer_token_here

# Optional: News API (only needed if NEWS_SENTIMENT_ENABLED=true)
# NEWS_API_KEY=your_api_key_here

# Logging Configuration
LOG_LEVEL=INFO

# Server Configuration
PORT=8000
HOST=0.0.0.0
